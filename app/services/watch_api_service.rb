class WatchApiService
  def self.get_watch_data
    # conn = Faraday.new(url: "https://rapidapi.com")
    # response = conn.get("/watches")

    # if response.status == 200
    #   JSON.parse(response.body)
    # else
    #   { error: "Unable to fetch watch data. Status code is #{response.status}." }
    # end
  end
end
